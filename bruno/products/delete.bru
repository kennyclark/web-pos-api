meta {
  name: delete
  type: http
  seq: 5
}

delete {
  url: {{process.env.BASE_URL}}/api/products/:id
  body: none
  auth: inherit
}

params:path {
  id: {{productId}}
}

script:post-response {
  bru.runner.setNextRequest(null)
}

settings {
  encodeUrl: true
  timeout: 0
}
