meta {
  name: get-by-id
  type: http
  seq: 3
}

get {
  url: {{process.env.BASE_URL}}/api/categories/:id
  body: none
  auth: inherit
}

params:path {
  id: {{categoryId}}
}

script:post-response {
  bru.runner.setNextRequest("update")
}

settings {
  encodeUrl: true
  timeout: 0
}
